{
    "buildCommand": "cd client-next && npm install && npm run build",
    "outputDirectory": "client-next/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "http://localhost:5000/api/:path*"
        }
    ]
}